[project]
name = "auto"
version = "0.1.0"
description = "AI tự vận hành browser — wrapper mỏng quanh browser-use"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.1.40",
    "python-dotenv>=1.2.1",
]
